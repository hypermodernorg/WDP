<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="WordDivisionPuzzles.Views.ItemDetailPage"
             Title="{Binding Title}">

    <!--<StackLayout Spacing="20" Padding="15">
        <Label Text="Divisor:" FontSize="Medium" />
        <Label Text="{Binding Item.Divisor}" d:Text="Item name" FontSize="Small"/>
        <Label Text="Quotient:" FontSize="Medium" />
        <Label Text="{Binding Item.Quotient}" d:Text="Item description" FontSize="Small"/>
    </StackLayout>-->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="- Delete Puzzle" Clicked="DeleteItem_Clicked" CommandParameter="{Binding Id}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid x:Name="NewGrid" Padding="30" BackgroundColor="Black">
        </Grid>

    </ContentPage.Content>



</ContentPage>